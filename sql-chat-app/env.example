# Copy to `.env` (either in `sql-chat-app/.env` or `sql-chat-app/backend/.env`)
#
# Backend (Django -> Hugging Face Gradio Space)
# GRADIO_SPACE defaults to "saadkhi/SQL_chatbot_API" if not set.
GRADIO_SPACE=saadkhi/SQL_chatbot_API

# Optional: set if your Space is private.
# HF_TOKEN=hf_xxx

# Note: `DEBUG` is used by docker-compose. Django settings currently hardcode DEBUG=True.
DEBUG=0

# Frontend (Vite)
# Optional override for the API base URL:
# VITE_API_BASE_URL=http://localhost:8000/api


